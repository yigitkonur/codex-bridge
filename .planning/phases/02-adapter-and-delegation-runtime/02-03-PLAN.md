---
phase: 2
plan: 02-03
name: Foreground/background delegation envelopes and monitoring
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - src/adapters/codex/index.mjs
  - src/codex-bridge.mjs
  - test/codex-adapter-lifecycle.test.mjs
  - test/bridge-static.test.mjs
  - plugin/scripts/codex-bridge.mjs
  - skill/scripts/codex-bridge.mjs
autonomous: true
requirements:
  - DLGT-01
  - DLGT-02
---

<objective>
Keep foreground and background task monitoring/retrieval reliable after moving dispatch behind the adapter.
</objective>

<tasks>
<task id="02-03-T1" type="execute">
<action>
Normalize adapter result lookup through persisted job state and stored result artifacts.
</action>
<acceptance_criteria>
- `adapter.getResult` resolves jobs through `resolveResultJob`.
- `result` command includes adapter-normalized result metadata.
</acceptance_criteria>
</task>

<task id="02-03-T2" type="execute">
<action>
Normalize adapter event streaming from session `.events` files.
</action>
<acceptance_criteria>
- `adapter.streamEvents` yields normalized events with tag, origin, raw line, and events path.
- Event tag parsing respects terminal tag headers.
</acceptance_criteria>
</task>

<task id="02-03-T3" type="execute">
<action>
Keep background jobs and worktree-auto state anchored to the launch workspace.
</action>
<acceptance_criteria>
- `task-worker` reads queued jobs from the original workspace root.
- `worktree-auto` exposes returned job id as registry task id.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `node --test test/codex-adapter-lifecycle.test.mjs test/bridge-static.test.mjs`
- Authenticated background task smoke: enqueue, wait, result, filtered events.
</verification>

<success_criteria>
- DLGT-02 is satisfied for background task enqueue, monitor, and result retrieval.
</success_criteria>
