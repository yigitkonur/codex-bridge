---
phase: 2
plan: 02-02
name: Runtime dispatch migration for supported Codex operations
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/adapters/codex/index.mjs
  - src/adapters/index.d.ts
  - src/codex-bridge.mjs
  - src/adapters/README.md
  - test/codex-adapter-lifecycle.test.mjs
  - test/bridge-static.test.mjs
  - plugin/scripts/codex-bridge.mjs
  - skill/scripts/codex-bridge.mjs
autonomous: true
requirements:
  - ADPT-01
  - ADPT-02
  - DLGT-01
---

<objective>
Route supported task execution through the backend adapter contract while preserving the existing Codex app-server runtime behavior.
</objective>

<tasks>
<task id="02-02-T1" type="execute">
<action>
Implement `adapter.dispatch` around `runAppServerTurn` with the options required by task execution.
</action>
<acceptance_criteria>
- Dispatch forwards prompt, cwd, session directory, model, effort, timeout, and turn options.
- Dispatch returns thread id, turn id, session dir, capabilities, and raw result.
</acceptance_criteria>
</task>

<task id="02-02-T2" type="execute">
<action>
Update `executeTaskRun` and `runBridgeTask` to use the selected adapter instead of directly invoking Codex turn helpers.
</action>
<acceptance_criteria>
- Foreground and background paths pass the same selected adapter into `executeTaskRun`.
- Static tests assert `adapter.dispatch` is the runtime call site.
</acceptance_criteria>
</task>

<task id="02-02-T3" type="execute">
<action>
Rebuild generated CLI bundles after source changes.
</action>
<acceptance_criteria>
- `skill/scripts/codex-bridge.mjs` and `plugin/scripts/codex-bridge.mjs` are generated from current source.
- `npm run baseline:contracts -- --check` accepts the generated surface.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `node --test test/codex-adapter-lifecycle.test.mjs test/bridge-static.test.mjs`
- `npm run build`
- `npm run baseline:contracts -- --check`
</verification>

<success_criteria>
- ADPT-01 is satisfied for task dispatch.
- DLGT-01 remains supported through the adapter dispatch path.
</success_criteria>
