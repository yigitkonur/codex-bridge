---
phase: 2
plan: 02-01
name: Adapter capability contract and backend precedence
type: execute
wave: 1
depends_on:
  - Phase 1
files_modified:
  - src/adapters/codex/index.mjs
  - src/adapters/index.d.ts
  - src/codex-bridge.mjs
  - src/lib/render.mjs
  - scripts/baseline-contracts.mjs
  - test/adapter-registry.test.mjs
  - test/adapter-selection.test.mjs
  - test/baseline-contracts.test.mjs
  - plugin/scripts/codex-bridge.mjs
  - skill/scripts/codex-bridge.mjs
autonomous: true
requirements:
  - ADPT-02
  - ADPT-03
  - ADPT-04
---

<objective>
Make backend capability state accurate, visible, and enforceable before migrating runtime commands through the adapter.
</objective>

<tasks>
<task id="02-01-T1" type="execute">
<action>
Update the Codex adapter capability contract so advertised optional lifecycle flags match implemented methods.
</action>
<acceptance_criteria>
- `supports_questions`, `supports_resume`, and `supports_steering` are true only with matching adapter methods present.
- Registry tests fail if optional lifecycle support is advertised without an implementation.
</acceptance_criteria>
</task>

<task id="02-01-T2" type="execute">
<action>
Expose backend and capability state in setup output in addition to version output.
</action>
<acceptance_criteria>
- `setup --json` returns `result.active_backend`.
- `setup --json` returns `result.adapter_capabilities`.
- Text setup rendering includes the active backend line.
</acceptance_criteria>
</task>

<task id="02-01-T3" type="execute">
<action>
Preserve and test backend precedence through explicit backend, environment, task metadata, routing, config layers, and defaults.
</action>
<acceptance_criteria>
- Existing adapter precedence tests remain passing.
- `task --backend unknown --json ...` fails with `BACKEND_INCAPABLE`.
- Baseline JSON probes include setup backend capability fields.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `node --test test/adapter-registry.test.mjs test/adapter-selection.test.mjs test/baseline-contracts.test.mjs`
- `CODEX_BRIDGE_NO_UPDATE_CHECK=1 node src/codex-bridge.mjs setup --json`
- `CODEX_BRIDGE_NO_UPDATE_CHECK=1 node src/codex-bridge.mjs task --backend unknown --json probe`
</verification>

<success_criteria>
- ADPT-02, ADPT-03, and ADPT-04 are satisfied for the shipping Codex backend.
</success_criteria>
