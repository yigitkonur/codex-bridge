---
phase: 2
plan: 02-04
name: Resume/respond/steer and timeout/handoff contracts
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - src/adapters/codex/index.mjs
  - src/adapters/index.d.ts
  - src/codex-bridge.mjs
  - test/codex-adapter-lifecycle.test.mjs
  - test/bridge-static.test.mjs
  - plugin/scripts/codex-bridge.mjs
  - skill/scripts/codex-bridge.mjs
autonomous: true
requirements:
  - DLGT-03
  - DLGT-04
---

<objective>
Route follow-up controls through adapter lifecycle methods and keep timeout/handoff behavior classified through the existing Codex runtime.
</objective>

<tasks>
<task id="02-04-T1" type="execute">
<action>
Implement `adapter.resume`, `adapter.respond`, `adapter.steer`, and `adapter.cancel`.
</action>
<acceptance_criteria>
- Resume injects the target thread id into turn options.
- Respond writes the pending-request response payload.
- Steer sends `turn/steer` with expected turn id.
- Cancel interrupts the active Codex turn when thread/turn ids are known.
</acceptance_criteria>
</task>

<task id="02-04-T2" type="execute">
<action>
Update CLI handlers to guard lifecycle capabilities and call adapter methods.
</action>
<acceptance_criteria>
- `send` guards `supports_resume` and calls `adapter.resume`.
- `respond` guards `supports_questions` and calls `adapter.respond`.
- `steer` guards `supports_steering` and calls `adapter.steer`.
- `cancel` calls `adapter.cancel`.
</acceptance_criteria>
</task>

<task id="02-04-T3" type="execute">
<action>
Preserve timeout and handoff classification from the existing Codex turn runtime.
</action>
<acceptance_criteria>
- Existing timeout/error tests remain passing.
- Adapter dispatch returns the raw turn result so CLI handlers preserve terminal/handoff metadata.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `node --test test/codex-adapter-lifecycle.test.mjs test/bridge-static.test.mjs`
- Authenticated foreground task smoke followed by `send` resume smoke.
- Existing timeout tests under the full Node suite.
</verification>

<success_criteria>
- DLGT-03 and DLGT-04 are satisfied for supported Codex lifecycle controls.
</success_criteria>
