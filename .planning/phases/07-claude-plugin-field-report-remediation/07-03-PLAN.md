# Phase 7.3: Final Verification And Closeout - Plan

## Goal

Prove the field-report remediation is complete, update planning state, and leave
the repository ready for the next real plugin smoke session.

## Tasks

1. Run full validation.
   - `npm run verify:static`
   - `git diff --check`

2. Audit P0-P2 coverage.
   - Update or create a phase summary mapping each field-report item to fixed,
     documented, or deferred.
   - Ensure any deliberately deferred items remain in planning with rationale.

3. Update GSD state.
   - Mark Phase 7 as completed in `.planning/STATE.md` and `.planning/ROADMAP.md`.
   - Move the field-report todo to completed only if all mandatory P0/P1 items
     are either fixed or explicitly deferred with replacement guidance.

## Verification

- Full static gate passes.
- Working tree contains only intentional source/docs/generated changes.

## Done Criteria

- Phase summary records concrete files changed and tests run.
- No open P0 remains.

