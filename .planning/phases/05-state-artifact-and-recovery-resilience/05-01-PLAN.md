# 05-01 Plan: Workspace State Concurrency And Canonical Root Proof

## Goal

Prove workspace state and session lookup stay anchored to the canonical workspace root when multiple processes or worktree paths interact with the same project.

## Tasks

1. Verify current state locking, atomic writes, canonical root hashing, stale-lock recovery, corrupt-state quarantine, and raw job listing contracts from source and tests.
2. Fix CLI session-directory call sites so relative `session_dir` values resolve against the workspace root, not whatever process cwd launched the command.
3. Add regression tests proving workspace-root-based session resolution and command call-site wiring.

## Deliverables

- `src/lib/session-log.mjs`: `resolveSessionDir(configDir, baseDir)` supports explicit workspace base.
- `src/codex-bridge.mjs`: task, review, wait, events, send, steer, respond, and summary pass workspace roots to `resolveSessionDir`.
- `test/session-log.test.mjs`: relative session directory resolution test.
- `test/bridge-static.test.mjs`: static call-site proof.

## Verification

- Targeted: `node --test test/session-log.test.mjs test/registry.test.mjs test/bridge-static.test.mjs`
- Full: `npm run build`, `npm run verify:static`, `git diff --check`
