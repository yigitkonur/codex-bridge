# 05-03 Plan: Cancel, Prune, Stale-Lock, Corrupt-State, And Missing-Artifact Recovery

## Goal

Ensure recovery-sensitive commands expose structured, retry-aware outcomes that orchestration code can consume without parsing human prose.

## Tasks

1. Preserve current stale-lock and corrupt-state behavior from `src/lib/state.mjs` and its tests.
2. Add structured `recovery` payloads to cancellation, orphan pruning, and missing/timeout artifact waits.
3. Convert invalid `respond --json-payload` input into a usage-classified CLI error rather than an internal parse crash.
4. Add static contract tests for recovery payload presence and invalid JSON handling.

## Deliverables

- `src/codex-bridge.mjs`: shared `buildRecovery` helper and recovery payloads for `cancel`, `status --prune-orphans`, and `await-artifact`.
- `src/codex-bridge.mjs`: `respond --json-payload` parse guard.
- `test/bridge-static.test.mjs`: recovery surface tests.

## Verification

- Targeted: `node --test test/session-log.test.mjs test/registry.test.mjs test/bridge-static.test.mjs`
- Full: `npm run build`, `npm run verify:static`, `git diff --check`
