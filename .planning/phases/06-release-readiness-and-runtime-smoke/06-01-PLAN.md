# 06-01 Plan: CI Generated Drift And Packaged Output Gates

## Goal

Make CI use the same static verification command maintainers run locally, while preserving explicit generated-bundle and packaged-output checks.

## Tasks

1. Wire the build workflow through `npm run verify:static`.
2. Keep generated bundle drift detection and packaged output existence checks in CI.
3. Add a static-mode runtime smoke harness invocation to prove the harness itself works without requiring Codex credentials in normal CI.
4. Add tests that pin the workflow behavior.

## Deliverables

- `.github/workflows/build.yml`
- `package.json`
- `scripts/runtime-smoke.mjs`
- `test/release-readiness.test.mjs`

## Verification

- `node --test test/release-readiness.test.mjs`
- `npm run verify:static`
