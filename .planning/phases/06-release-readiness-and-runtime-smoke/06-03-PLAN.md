# 06-03 Plan: Authenticated Runtime Smoke And Update Diagnostics

## Goal

Provide a release-time command that proves setup, task, result, event, and review behavior against a real Codex install, and keep update diagnostics covered by deterministic tests.

## Tasks

1. Add a runtime smoke harness with static-only, optional live, and `--require-codex` fail-closed modes.
2. In live mode, run setup, foreground task, result, events, and adversarial review probes in an isolated temporary workspace.
3. Preserve update check and auto-apply diagnostics through existing tests and include them in release-readiness coverage.
4. Run the live smoke locally when Codex CLI and app-server are available.

## Deliverables

- `scripts/runtime-smoke.mjs`
- `package.json`
- `test/release-readiness.test.mjs`
- Existing update tests retained: `test/update-command.test.mjs`, `test/auto-apply.test.mjs`

## Verification

- `npm run smoke:runtime -- --require-codex --json`
- `node --test test/release-readiness.test.mjs test/update-command.test.mjs test/auto-apply.test.mjs`
- `npm run verify:static`
